{
  "name": "helium-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cross-env PORT=10015 NODE_OPTIONS=--openssl-legacy-provider react-scripts start",
    "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider react-scripts build",
    "test": "cross-env NODE_OPTIONS=--openssl-legacy-provider react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "autoprefixer": "^10.4.24",
    "axios": "^1.13.5",
    "postcss": "^8.5.6",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.3.0",
    "react-scripts": "^0.0.0",
    "tailwindcss": "^4.2.0"
  },
  "devDependencies": {
    "@types/react": "^17.0.91",
    "@types/react-dom": "^17.0.26",
    "@types/react-router-dom": "^5.3.3",
    "cross-env": "^10.1.0"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
